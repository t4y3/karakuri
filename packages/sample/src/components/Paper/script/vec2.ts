// type Vector = {
//   x: number;
//   y: number;
// };
//
// export const length = (x: number, y: number) => {
//   return Math.sqrt(x ** 2 + y ** 2);
// };
//
// export const normalize = (x: number, y: number) => {
//   const l = length(x, y);
//   return { x: x / l, y: y / l };
// };
//
// export const dot = (a: Vector, b: Vector) => {
//   return a.x * b.x + a.y * b.y;
// };
